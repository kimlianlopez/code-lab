import React from "react"

import Layout from "../components/Layout"

const IndexPage = ({ location }) => {
  return (
    <Layout title="Oops!" location={location}>
      <div>Oops! Nothing here.</div>
    </Layout>
  )
}

export default IndexPage
